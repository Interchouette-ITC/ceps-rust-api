[package]
name = "ceps-rust-api-mcp"
version = "1.0.0"
edition = "2021"
authors = ["Interchouette <contact@interchouette.net>"]
rust-version = "1.85"
license = "MIT"
description = "MCP server for ceps-rust-api (stdio + Streamable HTTP)"
readme = "README.md"

# Standalone package (not a member of the repo workspace).
[workspace]

[[bin]]
name = "ceps-rust-api-mcp"
path = "src/main.rs"

[lib]
name = "ceps_rust_api_mcp"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
axum = { version = "0.8", default-features = false, features = [
    "http1",
    "tokio",
    "json",
] }
clap = { version = "4", features = ["derive", "env"] }
reqwest = { version = "0.13", default-features = false, features = [
    "json",
    "query",
    "rustls",
] }
rmcp = { version = "3.1.2", default-features = false, features = [
    "server",
    "macros",
    "transport-io",
    "transport-streamable-http-server",
] }
schemars = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = [
    "macros",
    "rt-multi-thread",
    "process",
    "time",
    "fs",
    "io-util",
] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
reqwest = { version = "0.13", default-features = false, features = [
    "json",
    "query",
    "rustls",
] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
opt-level = "s"
